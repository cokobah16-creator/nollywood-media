/* =========================================================================
   NACECA · src/main.js
   Entry point. The game's window-load handler lives in systems/boot.js;
   this file is the place to add post-boot wiring (analytics, telemetry,
   feature flags) without touching the engine layer.
   ========================================================================= */
